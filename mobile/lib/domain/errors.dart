/// Stable application failure identifiers, independent of display language.
enum CareErrorCode {
  invalidContextSelection,
  contextCancelled,
  contextTooLarge,
  locked,
  busy,
  languageSaveFailed,
  existingPinRequired,
  deviceAuthDisabled,
  deviceAuthFallback,
  deviceAuthIncomplete,
  patientNotFound,
  notificationPermissionDenied,
  noteTooLong,
  requiredField,
  fieldTooLong,
  invalidNumber,
  invalidChoice,
  noteRequired,
  backupPasswordTooShort,
  passwordMismatch,
  pinConfirmationMismatch,
  invalidEncryptedFile,
  invalidBackupSignature,
  draftPatientMismatch,
  invalidStorageKey,
  encryptedStorageUnavailable,
  recordPatientMismatch,
  storageIntegrityFailed,
  recordDetailMissing,
  unsupportedBackupVersion,
  invalidBackupTables,
  invalidBackupFields,
  invalidBackupValue,
  invalidBackupTimestamp,
  invalidBackupRevision,
  invalidPinFormat,
  pinThrottled,
  pinConfigurationInvalid,
  pinMismatch,
  timeZoneUnavailable,
  photoTooLarge,
  backupSaveCancelled,
  backupFileTooLarge,
  newerStorageVersion,
  storageVersionMismatch,
  unknownStorageFormat,
  invalidBackupSelection,
  selectedPatientMissing,
  scopeMismatch,
  draftEditorMismatch,
  checkinRequired,
  legacyBackupPhotosTooLarge,
  legacyBackupTooLarge,
  unsupportedBackupFormat,
  invalidBackupKeys,
  incompleteBackupStorage,
  backupPhotoLinkMismatch,
  backupPhotosTooLarge,
  backupSelectionTooLarge,
  duplicateBackup,
  emptyBackup,
  invalidBackupIds,
  invalidBackupPhotoSize,
  invalidBackupPhotoKey,
  backupSourceMissing,
  backupKindMismatch,
  invalidBackupRole,
  unsupportedEntryKind,
  invalidPlanTimes,
  revisionSourceMismatch,
  currentPlanMissing,
  persistedChatRetentionRequired,
  invalidQuestionLength,
  invalidDraftScope,
  draftRetentionRequired,
  draftSourceMismatch,
  draftTooLong,
  draftExpired,
  draftScopeMismatch,
  checkinDraftMustBeNew,
  draftConflict,
  medicationNameRequired,
  invalidIntakeTimes,
  medicationConflict,
  intakeStatusRequired,
  duplicateScheduledIntake,
  chatRetentionRequired,
  invalidAuthorRole,
  invalidEntryTime,
  entryConflict,
  entryKindImmutable,
  visitTitleRequired,
  unsupportedPhoto,
  invalidPhoto,
  photoResolutionTooLarge,
  animatedPhotoUnsupported,
  photoDecodeFailed,
  photoNotFound,
  storageLocationInvalid,
  storageKeyMissing,
  storageFileMissing,
  attachmentDeleteInvalid,
  reminderIdCollision,
  taskTitleRequired,
  unknownFailure,
}

final class CareError implements Exception {
  CareError(this.code, {List<String> labels = const []})
    : labels = List.unmodifiable(labels);
  final CareErrorCode code;
  // Only field metadata is accepted here; never patient-entered content.
  final List<String> labels;
  @override
  String toString() => 'CareError(${code.name})';
}
