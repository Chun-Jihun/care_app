# Project Instructions

## Source of truth

- 작업을 계획하거나 코드를 수정하기 전에
  `docs/caregiving_notebook_requirements.md`를 전체적으로 읽는다.
- 해당 문서를 제품 요구사항의 원본으로 취급한다.
- 문서와 충돌하는 구현이나 가정을 하지 않는다.
- 요구사항이 불명확하면 임의로 결정하지 말고 사용자에게 질문한다.

## Product purpose

- 이 제품은 의사나 간호사를 대체하지 않는다.
- 가족 및 비전문 간병인의 일상적인 간병 질문과 정보 이해를 돕는다.
- 진단, 처방, 약물 변경 또는 치료 결정을 제공하지 않는다.
- 위험한 상황에서는 앱 사용을 지속시키지 말고 의료기관 연락을 안내한다.

## Initial scope

- 초기 MVP에서는 환자 식별정보를 수집하거나 장기 저장하지 않는다.
- 가족 간 의료정보 공유와 병원·주치의 전송은 최후순위 기능이다.
- 개인화에는 현재 질문에 필요한 최소한의 비식별 맥락만 사용한다.
- 기본적으로 세션 종료 후 질문 맥락을 삭제한다.

## Medical accuracy

- 의료 정확성과 환각 방지가 모델 크기, 속도 및 대화 자연스러움보다 우선한다.
- 승인된 내부 의학 문서에 없는 의료 내용을 생성하지 않는다.
- 근거가 없거나 충돌하면 추측하지 않고 답변을 보류한다.
- 약물 용량, 상호작용 및 위험 임계값은 LLM이 자유 생성하지 않는다.
- 고위험 판단은 규칙 엔진이 LLM보다 먼저 처리한다.

## RAG and citations

- 허용 목록에 등록되고 의료진이 검수한 문서만 검색한다.
- 인터넷 검색 결과를 자동으로 의학 지식베이스에 추가하지 않는다.
- 의학적 핵심 주장마다 다음 출처 정보를 연결한다.
  - 자료명
  - 발행기관
  - 발행일 또는 개정일
  - 페이지 또는 장·절
  - 근거문장
  - 원문 링크
  - 앱 내부 검수일
- 출처 번호만 그럴듯하게 생성하는 것을 허용하지 않는다.

## Development rules

- 기능 구현 전에 안전 실패 시나리오와 테스트를 먼저 정의한다.
- 모델 평가와 검색기 평가를 분리한다.
- 모델, 임베딩, 검색 인덱스 또는 프롬프트가 변경되면 의료 회귀시험을 실행한다.
- 개인정보 저장이나 외부 전송 기능은 명시적인 승인 없이 구현하지 않는다.